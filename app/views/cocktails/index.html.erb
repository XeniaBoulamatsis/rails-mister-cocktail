  <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.maennersache.de/cocktails,id=33d51656,b=maennersache,w=1100,rm=sk.jpeg);">
    <div class="container">
      <h1>Misses Cocktail!</h1>
      <p>Best cocktails for people who like to get drunk 💃.</p>
    </div>
  </div>

  <div class="container">
    <h2>All cocktails</h2>
    <div class="row">
      <% @cocktails.each do |cocktail| %>
        <div class="col-3 my-3 card-wrapper">
          <%= link_to cocktail_path(cocktail) do %>
            <div class="card">
              <img class="card-img-top" src="<%=cocktail.url%>"  alt="...">
              <h5 class="card-title"><%= cocktail.name %></h5>
              <%= link_to "Delete", cocktail_path(cocktail), class: "btn btn-secondary",
                method: :delete,
                data: { confirm: "Are you sure?" } %>
              </div>
          <% end %>
        </div>
      <% end %>
    </div>
    <div class="row">
      <%= link_to 'Add cocktail', new_cocktail_path, class: "btn btn-secondary" %>
    </div>
  </div>


